package Interfaces;

/*
 * We can define the methods on which the objects work by using Interfaces
 * By this, we would be restricting the objects to work on interface methods
 */
public interface IMachine {

	public void StartMachine();
	public void StopMachine();
}
